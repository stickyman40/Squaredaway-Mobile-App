# Reference values for local development.
# The iOS app does not auto-load this file.
# Copy these into your Xcode scheme environment variables
# or export them in your shell before running related tools.

SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_ANON_KEY=your-anon-key
SUPABASE_REDIRECT_URL=squaredaway://auth-callback
